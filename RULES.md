# YouTube 歌枠时间戳歌轴 统一清洗规则（RULES.md）

> 本文件是 **唯一权威规则源**，由 DeepSeek 从「本地规则 vs DS 结果」的差异样本中提炼
> （`extract_rules.py` → `data/rules_from_ds.json`），供以下端共用：
> - **本地管道**（yt-comment-automation Python 实现，`yt_comment_automation/clean.py`）
> - **油猴插件**（YouTube 评论纯文本复制 + AI整理 user.js）
> - **其他项目**（future：需按平台语言移植，规则语义保持一致）

规则编号 `R01`~`R16`。每条含：行为、正则（JS/Python 兼容，避开 `\p` 等差异语法）、示例、保护项。

---

## 0. 通用原则（所有规则的前提）

1. 只保留「同时有歌名 + 歌手」的条目（B 站评论场景放宽：无歌手也可保留歌名，见 R17）。
2. 时间戳只取该歌曲的**开始时间**（同行起止 `38:12~43:24` 取 38:12）。
3. 无置信来源不输出：只有时间戳无歌名、只有歌手无歌名、纯聊天/杂谈行一律跳过。
4. 输出格式统一：`时间戳 NN. 歌名 - 歌手`（无歌手：`时间戳 NN. 歌名`）；编号 01 起连续；无空行。
5. 所有正则默认兼容 JS 与 Python（不用 `\p{...}`、不用命名组差异语法）。

---

## 规则清单

### R01 全角数字转半角 `normalize`
- 行为：`０-９` → `0-9`（用于时间戳、序号、备注括号）。
- 正则：`[０-９]`
- 示例：`１・２・３` → `1・2・3`；`オリジナル１（少し）` → `オリジナル1（少し）`
- 保护：规范化不删除内容；歌名本身用全角数字转换后语义不变。

### R02 剥离行首数字序号 `delete_prefix`
- 行为：剥离 `01.` `1.` `2)` 等序号；数字后必须跟标点且**后面不是数字**（避免误伤 8.32）。
- 正则：`^[0-9０-９]+[.、.)）．](?![0-9０-９])\s*`
- 示例：`01. 'PPPP' / TAK` → `'PPPP' / TAK`；`8.32` 不动（点后是数字）。
- 保护：纯数字歌名 `8.32`、`441` 不剥离。

### R03 剥离全角括号序号 `delete_prefix`
- 行为：剥离 `〔10〕` 前缀序号。
- 正则：`^〔[0-9０-９]+〕\s*`
- 示例：`〔10〕 JANE DOE` → `JANE DOE`
- 保护：括号内是汉字/日文的正式名（如 `〔雨〕`）不动。

### R04 剥离行首列表装饰符 `delete_prefix`
- 行为：剥离 `└ ├ │ ◆ ☆ → ・` 等装饰符（树形 setlist 行）。
- 正则：`^[└├│▸▪●○◆◇☆★➔→・]\s*`
- 示例：`└  Blue Jeans / HANA` → `Blue Jeans / HANA`
- 保护：不剥离 `/`/`//`（DISH// 安全）；`♡` 不在字符类（ツインテールは20歳まで♡ 安全）。

### R05 剥离歌名最外层成对单引号 `normalize`
- 行为：`'PPPP'` → `PPPP`（引号是标注不是歌名内容）。
- 正则：`^'([^']*)'`
- 示例：`'PPPP' / TAK` → `PPPP / TAK`
- 保护：`Don't say "lazy"` 不以单引号开头、内部双引号不删；`'Til Tuesday` 只有开引号不匹配。

### R06 删除歌名末尾译文/罗马字括号 `delete_suffix`
- 行为：删除末尾括号内含**空格或 `|`** 的罗马字/英文翻译注释。
- 正则：`\s*[(（][^)）]*(?:\s|\|)[^)）]*[)）]\s*(?=/|$)`
- 示例：`絶頂讃歌 (Zetchou Sanka | Orgasm Anthem) / 和ぬか` → `絶頂讃歌 / 和ぬか`；
  `逆光 (Gyakkou | Backlight) / ウタ (Uta)` → `逆光 / ウタ (Uta)`
- 保护：括号内无空格/竖线的正式名 `ryo(supercell)`、`ナノウ(ほえほえP)` 不删。

### R07 删除歌名末尾表演备注括号 `delete_suffix`
- 行为：删除 `（少し）（ちょっと）（1番のみ）（練習）（うろ覚え）` 等"只唱一部分"备注。
- 正则：`\s*[（(](?:少し|ちょっと|うろ覚え|練習|[0-9０-９]+番のみ)[）)]\s*(?=/|$)`
- 示例：`ロビンソン（少し）/スピッツ` → `ロビンソン /スピッツ`；
  `瞬き（1番のみ）/back number` → `瞬き /back number`；
  `ウィステリア（ちょっと）/Elinica` → `ウィステリア /Elinica`
- 保护：括号内容为 `supercell` 等正式名不匹配；不删歌名中的 `！`。

### R08 删除非歌曲行 `normalize`
- 行为：删除结束语 `締め`、点歌请求 `1,2曲リクエスト` 等噪音行。
- 正则：`^(?:締め|[0-9０-９]+[,，][0-9０-９]+曲リクエスト)$`
- 示例：`締め` → 丢弃；`1,2曲リクエスト` → 丢弃；`リクエスト`（歌名）保留。
- 保护：单独歌名 `リクエスト` 不匹配 `曲リクエスト`。

### R09 保护正式标题符号 `keep`
- 行为：以下符号/结构是歌名一部分，任何清洗步骤不可剥离。
- 正则：`(?:♡|//|\(supercell\)|"[^"]*")`
- 示例：`ツインテールは20歳まで♡`、`Don't say "lazy"`、`ryo(supercell)`、`DISH//` 全部保留。

### R10 全角 ASCII 字母/数字统一半角 `normalize`
- 行为：`０-９Ａ-Ｚａ-ｚ` → 半角（解析时间戳/序号/备注前提）。
- 正则：`[０-９Ａ-Ｚａ-ｚ]`
- 示例：`ＬＩＦＥ（１番のみ）` → `LIFE（1番のみ）`；`０１. RE:I AM` → `01. RE:I AM`
- 保护：不转换日文假名、汉字、全角标点。

### R11 只把带时间戳+artist 分隔符的行当歌曲行 `keep`
- 行为：行须含时间戳且含 `/` 或 `／`（分隔符前后有内容）；`/` 前为歌名、后为歌手。
  无时间戳的聊天、有时间戳但无 artist 分隔符的聊天行忽略。
- 正则：`^(?:[┗┣━─\-|>\s]*)(?:\d{1,2}:\d{2}(?::\d{2})?)\s*[:：]?\s*(?:[0-9０-９]{1,3}\s*[.．:：]\s+)?(.+?)\s*(?<![／/])[／/](?![／/])\s*(.+)$`
- 示例：`┗2:27:08 紙様／Creepy Nuts` → `紙様 / Creepy Nuts`；聊天行忽略。
- 保护：`DISH//` 双斜杠不是分隔符；`ryo(supercell)` 括号不拆。

### R12 同一时间重复候选优先取带序号行 `normalize`
- 行为：同一时刻（秒差 ≤2s）出现重复候选时，优先取带序号前缀的歌曲行，避免选中聊天行。
- 正则：`^(?:[┗┣━─\-|>\s]*)(?:\d{1,2}:\d{2}(?::\d{2})?)\s*[:：]?\s*[0-9０-９]{1,3}\s*[.．:：]\s+(.+?)\s*(?<![／/])[／/](?![／/])\s*.+$`
- 示例：`0:30:03　05. SLOW FLAME / HONK THE HORN` 优先于 `00:30:04 SLOW FALME / ...`
- 保护：无带序号行时保留原行，不强行纠错。

### R13 剥离标题前数字序号 `delete_prefix`
- 行为：剥离 `01.` `16.` `05.`（仅当序号后跟空格或非数字）。
- 正则：`^\s*[0-9０-９]{1,3}\s*[.．:：](?:\s+|(?=[^0-9０-９\s]))`
- 示例：`01. RE:I AM` → `RE:I AM`；`16. 奏（かなで）` → `奏（かなで）`
- 保护：`RE:I AM` 的 `RE:` 不删；`8.32` 纯数字歌名不删。

### R14 删除歌名末尾注音假名括号 `delete_suffix`
- 行为：删除末尾括号内**全为假名**的注音（如 `奏（かなで）` 中括号是读音说明）。
- 正则：`\s*[（(][ぁ-んァ-ヶー]+[）)]\s*(?=/|$)`
- 示例：`奏（かなで）` → `奏`（若为注音）；`(恋は)百年戦争` 前置括号不删。
- 保护：括号内含汉字的正式名（`涼宮ハルヒ(平野綾)`）不删。

### R15 删除歌名末尾发行日期/元数据括号 `delete_suffix`
- 行为：删除 `(2019/05/05)`、`[2018年]` 等发行日期。
- 正则：`\s*[\[（(]\s*(?:18|19|20|21)\d{2}\s*(?:年)?\s*[\]）)]?\s*(?=/|$)`
- 示例：`Aimer (2019/05/05)` → `Aimer`；`橋本潮 (Ushio Hashimono)` 属 R06。
- 保护：不含年份的正式括号不删。

### R16 歌手字段统一
- 行为：歌手 `さん/様/氏` 敬语（多数统一时）批量去除；汉字名内部空格压缩（`奥 華子`→`奥華子`）。
- 示例：`歌手さん` ×3 + `歌手` → 全部 `歌手`；`奥 華子` → `奥華子`
- 保护：`Never young beach` 等英文名空格保留。

### R17 无歌手条目（B站评论场景）
- 行为：允许输出只有歌名的条目（`时间戳 NN. 歌名`），不做歌手补全。
- 触发：B站评论管道默认开启；**油猴插件强制要求歌手**（`REQUIRE_ARTIST=1` 时仅输出有歌手行）。
- 保护：只有歌手没有歌名、或两者皆无的行仍跳过。

---

## 同步约定

- **权威源**：本 RULES.md + `data/rules_from_ds.json`（机器可读版，含 regex）。
- 本地管道：`yt_comment_automation/clean.py` 内已实现 R01-R07/R11/R16 等主体，
  未实现项按 `rules_from_ds.json` 增量补齐。
- 油猴插件：user.js 中 `stripTrailingArtistNotes`、`stripTrailingLatinAnnotationSuffix`
  等函数同步 R06/R07/R14 语义；插件强制歌手（R17 关闭）。
- 新增差异样本后重跑 `python extract_rules.py` 更新 `data/rules_from_ds.json`，
  人工复核后回填本文件。
